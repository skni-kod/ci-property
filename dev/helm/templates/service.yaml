apiVersion: v1
kind: Service
metadata:
  name: {{ include "steel-dart.fullname" . }}
  labels:
    {{- include "steel-dart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "steel-dart.selectorLabels" . | nindent 4 }}
