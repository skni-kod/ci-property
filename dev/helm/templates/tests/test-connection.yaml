apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "steel-dart.fullname" . }}-test-connection"
  labels:
    {{- include "steel-dart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "steel-dart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
